# CONVENTIONS — قواعد ملزمة

> هذا الملف يُمرَّر للنموذج في كل مهمة. أي مخالفة = رفض العمل وتراجع كامل.

---

## 1. قواعد التسمية (منع التعارض)

- ممنوع تسمية أي كلاس باسم موجود في Flutter أو Dart core.
  الأسماء المحظورة صراحةً:
  `ErrorWidget`, `User`, `Notification`, `State`, `Route`, `Page`,
  `Image`, `Text`, `Icon`, `Card`, `Table`, `Form`, `Action`, `Builder`,
  `Size`, `Color`, `Border`, `Padding`, `Center`, `Align`, `Flow`.
- استخدم بادئة المشروع بدلاً منها:
  `AppErrorWidget`, `AppUser`, `AppNotification`, `AppCard`.
- قبل إنشاء أي كلاس جديد: ابحث عن الاسم في `lib/` أولاً. إن وُجد — أعد استخدامه ولا تنشئ نسخة ثانية.

---

## 2. ممنوع الاختراع

- ممنوع استخدام أي كلاس أو دالة أو ثابت لم تتحقق من وجوده فعلياً في الملفات المقروءة.
- إن احتجت شيئاً غير موجود: أنشئه صراحةً في ملف مستقل، أو أوقف المهمة واطلب توضيحاً.
- ممنوع اختراع أسماء مثل `FunctionException` أو `MyApp` دون تعريفها.

---

## 3. الحزم والأصول

- ممنوع إضافة أي حزمة إلى `pubspec.yaml` دون إذن صريح.
- ممنوع إضافة خط أو صورة إلى `pubspec.yaml` ما لم يكن الملف موجوداً فعلاً على القرص.
- ممنوع تغيير إصدارات الحزم الموجودة.

---

## 4. البنية

- طبقة واحدة لكل مسؤولية:
  `lib/models/` — بيانات فقط، بلا منطق شبكة
  `lib/services/` — كل نداءات Supabase
  `lib/screens/` — واجهات فقط
  `lib/widgets/` — مكونات قابلة لإعادة الاستخدام
- ممنوع استدعاء Supabase مباشرة من داخل شاشة أو ودجت.
- التوجيه (go_router) يُعرَّف في ملف واحد فقط. ممنوع إنشاء ملف توجيه ثانٍ.
- كل مسار جديد يُضاف إلى ملف التوجيه الموجود، لا إلى ملف جديد.

---

## 5. الأمان — خط أحمر

ممنوع منعاً باتاً، ويُوقف العمل فوراً إن لزم:

- كتابة أو تعديل أي دالة SQL أو سياسة RLS.
- تعديل أي شيء يتعلق بالأدوار أو الصلاحيات (`role`, `is_admin`, `permissions`).
- قبول حقل دور من جهة العميل في التسجيل أو تحديث الملف الشخصي.
- وضع أي مفتاح أو رمز سري داخل الكود.
- استخدام `service_role` key في كود التطبيق.

أي مهمة تمس ما سبق: ضع عليها `[!]` وتوقف وانتظر المراجعة البشرية.

---

## 6. حجم التغيير

- حد أقصى 5 ملفات معدّلة في المهمة الواحدة.
- حد أقصى 300 سطر مضاف في المهمة الواحدة.
- إن كانت المهمة أكبر: قسّمها واكتب التقسيم في `PROGRESS.md` ولا تنفّذ.

---

## 7. الاختبارات

- لا تكتب اختباراً يستخدم صنفاً وهمياً (mock) لواجهة لم تقرأها.
- إن فشل اختبار قديم بسبب تغييرك: أصلح الاختبار، ولا تحذفه.
- ممنوع حذف أي ملف اختبار.

---

## 8. النظافة

- ملف واحد يحتوي دالة `main` واحدة فقط.
- كل تعليمات `import` في أعلى الملف، قبل أي تعريف.
- لا تترك كوداً معلّقاً أو `TODO` جديداً دون تسجيله في `TODO.md`.

---

## 9. عند الشك

توقف واكتب سؤالك في `PROGRESS.md` تحت عنوان `## NEEDS CLARIFICATION`.
التوقف أرخص من كود خاطئ.

---

## دروس مستفادة تلقائياً

- تصحيح لدرس سابق كان خاطئاً: فحص السلامة المعمارية (gate_sanity) يقارن فعلياً مع خط أساس (`state/sanity.base`) ويُبلّغ فقط عن مخالفات *جديدة* أدخلها التغيير الحالي — إن ظهرت مخالفة في ملف لم تذكره المهمة إطلاقاً (مثل ملف Dart غير مرتبط بمهمة إنشاء ملف JS)، فهذا يعني أن النموذج نفسه أدخلها بمحاولته "إصلاح" شيء لم يُطلب منه؛ التزم حرفياً بالملف/الملفات المذكورة في نص المهمة فقط ولا تُصلح أي مخالفة أخرى مهما بدت متعلقة.
- مهم جداً: أداة الطابور (`current_task()` في auto_pilot.sh) تقرأ من TODO.md **سطراً واحداً فقط** (السطر الذي يبدأ بـ `- [ ] `) ولا تمرّر أي محتوى بعده (كتل شيفرة داخل ```)  إلى aider. إن كانت المهمة تحتاج محتوى ملف كامل ودقيق (JS/HTML/CSS طويل)، يجب أن يُكتب ذلك المحتوى بالكامل داخل نفس السطر الواحد أو يُنفَّذ مباشرة خارج هذا الطابور — وإلا فسيصل النموذج نصاً مبتوراً وسيضطر للتخمين، وهذا هو السبب الجذري وراء عدة مهام محظورة سابقاً وليس أي عيب في الملفات المطلوبة نفسها.
- أي إشارة من الفحص الآلي بأن "التغيير يمس الصلاحيات أو الأمان" تُوقف المهمة فوراً حتى لو كان الملف غير حسّاس ظاهرياً (كملف CSS أو JS بسيط)؛ لا تُعِد المحاولة بنفس المحتوى ولا تُوسّع التغيير ولا تدمجه مع تغييرات أخرى، واطلب مراجعة بشرية.
- النمط: مخالفة طبقات/أمان واحدة قائمة في ملف (مثل نداء Supabase مباشر داخل شاشة) تُرفض بسببها كل المهام اللاحقة حتى التي لم تذكر ذلك الملف؛ عالجها في مهمة مخصّصة واحدة أولاً واطلب مراجعة بشرية، ولا تواصل تنفيذ المهام الأخرى ولا تُوسّع تعديلاتك في ذلك الملف لمحاولة إسكات الفحص.
- النمط: إضافة عمود أو أي تغيير في مخطط قاعدة البيانات يجب أن يقتصر على ملف migration جديد واحد فقط، مع تأجيل تعديلات الموديلات/الخدمات/الشاشات/الاختبارات إلى مهام لاحقة منفصلة؛ دمجها كلها في مهمة واحدة هو ما أدى إلى تجاوز حد الـ5 ملفات.
- النمط: المهمة التي تحدد ملفاً واحداً صراحةً ("عدّل/أنشئ هذا الملف فقط") يجب ألا تنشئ أو تعدّل أي ملف آخر كأثر جانبي (migration، موديل، خدمة، اختبار، توثيق)؛ هذا التوسّع الجانبي هو ما بلغ 12 ملفاً وتجاوز الحد.
- النمط: تجميع عدة إصلاحات غير مترابطة في مهمة واحدة (مثل «أصلح هذه الأخطاء الثلاثة في ملف واحد») يجعل فشل بند واحد يُسقط المهمة كلها ويجعل التراجع غامضاً؛ اجعل كل خطأ أو تغيير منطقي في مهمة مستقلة بذاته.
- النمط: المهمة المعرَّفة بمعيار يمس المشروع كله (مثل «احذف كل الاستيرادات غير المستخدمة التي يرصدها flutter analyze») بلا قائمة ملفات مسمّاة لا يمكن حصرها ضمن حد الـ5 ملفات؛ استبدلها بمهام محدَّدة الملفات، مهمة لكل ملف على حدة.
